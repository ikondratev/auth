ru:
  model:
    errors:
      user:
        name:
          blank: 'No empty name required'
          format: 'Wrong name type'
        email:
          blank: 'No empty email param required'
        password:
          blank: 'No empty password param required'
      user_session:
        uuid:
          blank: 'No empty uuid required'